# Database Configuration (Required)
# Use 'file:./dev.db' for local development or 'file:/app/prisma/dev.db' for Docker
DATABASE_URL="file:./data/db/prod.db"

# OpenAI API Key (Required for AI Features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# AI Provider Configuration (Optional)
# AI_PROVIDER="openai" # or "ollama"
# OLLAMA_BASE_URL="http://localhost:11434/v1" # or "http://host.docker.internal:11434/v1" in Docker
# OLLAMA_MODEL="llama3.1"

# Data Directory (Optional)
# Directory where configuration and runtime data is stored.
# Defaults to "./data" locally or "/app/data" in Docker.
# DATA_DIR="./data"

# SuperAdmin Password (Optional - First deployment only)
# Used when creating the SuperAdmin account on first run
# Login ID: SUPER001 | Default password: superadmin123
# ADMIN_PASSWORD="superadmin123"

# Security (Required for Session Encryption)
SESSION_SECRET="ensure-this-is-at-least-32-characters-long-in-prod"

# License Key (Optional - For Commercial Deployments)
# Get this from the author after purchasing a license.
# Without a valid key, the system operates in TRIAL mode (1 product line).
LICENSE_KEY=""


